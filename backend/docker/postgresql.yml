version: '2'
services:
  ava-postgresql:
    image: postgres:11.5
    # volumes:
    #     - ~/volumes/jhipster/srsservice/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=ava
      - POSTGRES_PASSWORD=ava
    ports:
      - 5432:5432

